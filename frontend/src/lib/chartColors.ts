// Two separate palettes, deliberately non-overlapping hue families, so the
// same color is never used for two different meanings across the dashboard's
// two breakdown cards -- e.g. index 0 in one chart isn't visually confusable
// with index 0 in the other.
export const LANGUAGE_PALETTE = ['#7c87ff', '#4fb0c6', '#5b64c9', '#8891d6', '#3ea8c9'];
export const CATEGORY_PALETTE = ['#3ecf8e', '#e6b450', '#f2545b', '#c77dff', '#8f98ab', '#e0779a'];
